# ABOUTME: CI workflow for the follow_repository package.
# ABOUTME: Runs tests, formatting, and analysis only when follow_repository files change.

name: Follow Repository CI

on:
  push:
    branches: [main]
    paths:
      - "mobile/packages/follow_repository/**"
      - ".github/workflows/follow_repository.yaml"
  pull_request:
    branches: [main]
    paths:
      - "mobile/packages/follow_repository/**"
      - ".github/workflows/follow_repository.yaml"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    uses: VeryGoodOpenSource/very_good_workflows/.github/workflows/flutter_package.yml@v1
    with:
      working_directory: "mobile/packages/follow_repository"
