# ABOUTME: CI workflow for the notification_repository package.
# ABOUTME: Runs tests, formatting, and analysis only when notification_repository files change.

name: Notification Repository CI

on:
  push:
    branches: [main]
    paths:
      - "mobile/packages/notification_repository/**"
      - ".github/workflows/notification_repository.yaml"
  pull_request:
    branches: [main]
    paths:
      - "mobile/packages/notification_repository/**"
      - ".github/workflows/notification_repository.yaml"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    uses: VeryGoodOpenSource/very_good_workflows/.github/workflows/flutter_package.yml@v1
    with:
      working_directory: "mobile/packages/notification_repository"
      min_coverage: 94
