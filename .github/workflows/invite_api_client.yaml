# ABOUTME: CI workflow for the invite_api_client package.
# ABOUTME: Runs tests, formatting, and analysis only when invite_api_client files change.

name: Invite API Client CI

on:
  push:
    branches: [main]
    paths:
      - "mobile/packages/invite_api_client/**"
      - ".github/workflows/invite_api_client.yaml"
  pull_request:
    branches: [main]
    paths:
      - "mobile/packages/invite_api_client/**"
      - ".github/workflows/invite_api_client.yaml"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    uses: VeryGoodOpenSource/very_good_workflows/.github/workflows/flutter_package.yml@v1
    with:
      working_directory: "mobile/packages/invite_api_client"
      min_coverage: 89
