# ABOUTME: CI workflow for the hashtag_repository package.
# ABOUTME: Runs tests, formatting, and analysis only when hashtag_repository files change.

name: Hashtag Repository CI

on:
  push:
    branches: [main]
    paths:
      - "mobile/packages/hashtag_repository/**"
      - ".github/workflows/hashtag_repository.yaml"
  pull_request:
    branches: [main]
    paths:
      - "mobile/packages/hashtag_repository/**"
      - ".github/workflows/hashtag_repository.yaml"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    uses: VeryGoodOpenSource/very_good_workflows/.github/workflows/flutter_package.yml@v1
    with:
      working_directory: "mobile/packages/hashtag_repository"
      min_coverage: 30
