<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Internet permission for loading videos and content -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Camera and Microphone Permissions for Video Recording -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Media/Storage Permissions for Profile Picture Selection -->
    <!-- For Android 13+ (API 33+) - Only images for profile pictures -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <!-- For Android 10-12 (API 29-32) - Only needed for profile pictures on older devices -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <!-- For saving videos to gallery on API 29 and below (gal package) -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="29" />

    <!-- Push Notifications Permission for Android 13+ (API 33+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <!-- Force override camera.any and wifi to NOT be required (dependencies set them to required=true) -->
    <uses-feature android:name="android.hardware.camera.any" android:required="false" tools:replace="android:required" />
    <uses-feature android:name="android.hardware.wifi" android:required="false" tools:replace="android:required" />

    <!-- P2P Permissions for Future Video Sharing (not yet implemented) -->
    <!-- Uncomment when P2P sync is enabled:
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES" />
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="false" />
    <uses-feature android:name="android.hardware.wifi.direct" android:required="false" />
    -->

    <!-- Declare that we do NOT use advertising ID for Google Play compliance -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID"
        tools:node="remove" />

    <!-- FORCE REMOVE location permissions added by dependencies -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"
        tools:node="remove" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"
        tools:node="remove" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION"
        tools:node="remove" />

    <!-- FORCE REMOVE P2P permissions added by nearby_connections/flutter_embedded_nostr_relay -->
    <uses-permission android:name="android.permission.BLUETOOTH"
        tools:node="remove" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        tools:node="remove" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE"
        tools:node="remove" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"
        tools:node="remove" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        tools:node="remove" />
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES"
        tools:node="remove" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"
        tools:node="remove" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"
        tools:node="remove" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE"
        tools:node="remove" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE"
        tools:node="remove" />

    <!-- FORCE REMOVE advertising permissions -->
    <uses-permission android:name="android.permission.ACCESS_ADSERVICES_ATTRIBUTION"
        tools:node="remove" />
    <uses-permission android:name="android.permission.ACCESS_ADSERVICES_AD_ID"
        tools:node="remove" />

    <application
        android:label="Divine"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/NormalTheme"
        android:localeConfig="@xml/locales_config"
        android:networkSecurityConfig="@xml/network_security_config">
        <!-- Render with Impeller. This reverses the Skia opt-out added in
             df2e479f7 ("perf(android): switch rendering backend to
             Skia"), and it is a traded risk rather than a fix landing on
             a solved problem - read this before assuming it is safe.

             That opt-out came from on-device profiling of profile builds
             during feed scroll:
             - Impeller/Vulkan: 17-27ms raster per frame, 37-46 FPS,
               constant jank (many blur/saveLayer passes over a live
               video texture).
             - Impeller/OpenGLES: 5-8ms raster, 60 FPS, but video
               textures intermittently rendered BLACK while scrolling to
               the next feed item - the Impeller GLES SurfaceTexture /
               external-texture bug class (flutter/flutter#141636,
               #145531).
             - Skia: 13-16ms raster, 60 FPS, correct video rendering.

             Nothing upstream has changed since. That measurement was
             taken on Flutter 3.44.0, the same pin this branch builds on,
             with both cited issues already closed - so this is not a
             re-evaluation after an engine upgrade. What changed is the
             requirement: the video editor's live chroma-key preview
             needs `ui.ImageFilter.shader`, which Skia does not implement
             at all. On Skia the preview degrades to the unkeyed video
             (the exported file is unaffected either way), and we decided
             a working preview is worth re-entering Impeller.

             Two things that follow, both easy to get wrong:

             - "Falls back to Skia" is not what happens. Below API 29 the
               engine does use Skia, but on API 29+ without a usable
               Vulkan context (Huawei, MediaTek below API 31, the engine's
               known-bad-SOC list) it falls back to Impeller *OpenGLES* -
               the backend the black-video-texture measurement above
               implicates. `ImageFilter.isShaderFilterSupported` is true
               there, so the editor's no-shader fallback does not engage
               on those devices either. If feed video renders black in the
               field, that device class is the first place to look, and
               this commit is the one to revert.
             - Re-check on every upgrade, the same way the original
               measurement was taken: profile build, feed scroll, watch
               raster time and whether video textures stay correct.

             There is deliberately no `EnableImpeller` entry below.
             Impeller is the engine default on API 29+, so the entry is
             redundant; setting it to `true` would also work and would not
             trip the "[Action Required]: Impeller opt-out deprecated"
             warning, which the engine only logs when Impeller is actually
             off. Verified on a Galaxy S25 (API 36, Vulkan 1.3): with no
             entry, `ImageFilter.isShaderFilterSupported` reports true,
             which is what the chroma-key preview needs. -->
        <!-- FCM default notification channel -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="openvine_push" />
        <meta-data
            android:name="google_analytics_automatic_screen_reporting_enabled"
            android:value="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize"
            android:screenOrientation="portrait">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Deep links for apex/www Divine hosts and the canonical login hostname -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>

                <data android:scheme="https" android:host="divine.video" />
                <data android:scheme="https" android:host="www.divine.video" />
                <data android:scheme="https" android:host="login.divine.video" />

                <data android:pathPrefix="/video"/>
                <data android:pathPrefix="/profile"/>
                <data android:pathPrefix="/hashtag"/>
                <data android:pathPrefix="/search"/>
                <data android:pathPrefix="/invite"/>
                <!-- pathPattern, not pathPrefix: a "/list" prefix would also
                     claim the web's /lists browse page (prefix matching is a
                     raw string prefix, not segment-aware). -->
                <data android:pathPattern="/list/.*"/>
                <data android:pathPrefix="/reset-password"/>
                <data android:pathPrefix="/verify-email"/>
            </intent-filter>
            <!-- NIP-46 callback: signer apps open divine:// to return to our app -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="divine" />
            </intent-filter>


        </activity>
        <activity
            android:name="zendesk.support.request.RequestActivity"
            android:theme="@style/ZendeskTheme" />
        <activity
            android:name="zendesk.support.requestlist.RequestListActivity"
            android:theme="@style/ZendeskTheme" />
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>

        <!-- NIP-55 Android signer (Amber) -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="nostrsigner" />
        </intent>

        <!-- Email app launch (email verification flow) -->
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.APP_EMAIL" />
        </intent>
    </queries>
</manifest>
