# time_formatter

Status: Current
Validated against: `pubspec.yaml` on 2026-03-19.

Purpose: small utility package for turning timestamps into user-facing relative or formatted strings.

Used by: feed, profile, inbox, and other UI surfaces that render times.

Test locally:

```bash
cd mobile/packages/time_formatter
dart test
```
