# keycast_flutter

Status: Current
Validated against: `pubspec.yaml` on 2026-03-19.

Purpose: Flutter client for Keycast OAuth and BYOK flows used by Divine account and authentication experiences.

Used by: auth and account-linking flows that rely on Keycast.

Test locally:

```bash
cd mobile/packages/keycast_flutter
flutter test
```
