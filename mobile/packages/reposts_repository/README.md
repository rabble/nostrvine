# reposts_repository

Status: Current
Validated against: `pubspec.yaml` on 2026-03-19.

Purpose: repository for Nostr generic reposts and repost-state coordination.

Used by: feed and profile surfaces that display or mutate repost state.

Test locally:

```bash
cd mobile/packages/reposts_repository
dart test
```
