name: sounds_repository
description: >
  Repository for fetching and caching audio events.
version: 0.0.1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  models:
    path: ../models
  nostr_client:
    path: ../nostr_client
  nostr_sdk:
    path: ../nostr_sdk
  rxdart: ^0.28.0
  unified_logger:
    path: ../unified_logger

dev_dependencies:
  mocktail: ^1.0.4
  test: ^1.26.3
  very_good_analysis: ^10.2.0
