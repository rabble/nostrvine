# pooled_video_player

Status: Current
Validated against: `pubspec.yaml` on 2026-03-19.

Purpose: pooled video playback package for short-form feed performance and controller reuse.

Used by: feed and playback surfaces where rapid scroll performance matters.

Test locally:

```bash
cd mobile/packages/pooled_video_player
flutter test
```
