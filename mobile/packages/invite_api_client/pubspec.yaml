name: invite_api_client
description: Invite API client and models for onboarding and invite sharing
version: 0.1.0+1
publish_to: none

environment:
  sdk: ^3.11.0

resolution: workspace

dependencies:
  crypto: ^3.0.7
  equatable: ^2.0.7
  http: ^1.5.0
  keycast_flutter:
  nostr_key_manager:
  nostr_sdk:

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
  very_good_analysis: ^10.2.0
