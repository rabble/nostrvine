# media_cache

Status: Current
Validated against: `pubspec.yaml` on 2026-03-19.

Purpose: reusable media-cache package with corruption recovery and optional manifest-aware lookups.

Used by: video and media playback flows that need resilient local caching.

Test locally:

```bash
cd mobile/packages/media_cache
flutter test
```
