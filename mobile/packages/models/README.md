# models

Status: Current
Validated against: `pubspec.yaml` on 2026-03-19.

Purpose: shared data models, JSON serialization targets, and cross-package value types.

Used by: the app and most workspace packages.

Test locally:

```bash
cd mobile/packages/models
dart test
```
