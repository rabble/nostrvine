/// App-specific tag name used to mark a video reply as feed-visible.
const String videoReplyVisibilityTagName = 'divine:reply_visibility';

/// Tag value indicating the reply should also appear in normal feeds.
const String videoReplyVisibilityFeedValue = 'feed';
