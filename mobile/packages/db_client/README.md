# db_client

Status: Current
Validated against: `pubspec.yaml` on 2026-03-19.

Purpose: local storage package for Drift-backed persistence and app-side cached data.

Used by: repository packages that need local reads, writes, and cache hydration.

Test locally:

```bash
cd mobile/packages/db_client
flutter test
```
