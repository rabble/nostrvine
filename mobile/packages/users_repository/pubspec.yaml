name: users_repository
description: Repository for fetching user profile information from Nostr.
version: 0.1.0+1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.10.0

dependencies:
  meta: ^1.11.0
  nostr_client:
    path: ../nostr_client
  nostr_sdk:
    path: ../nostr_sdk

dev_dependencies:
  mocktail: ^1.0.4
  test: ^1.26.3
  very_good_analysis: ^10.0.0
