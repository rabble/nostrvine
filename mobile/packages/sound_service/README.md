# sound_service

Status: Current
Validated against: `pubspec.yaml` on 2026-03-19.

Purpose: audio playback and sound-effect services, including countdown and in-app playback helpers.

Used by: recording and editing flows that need app-managed sound playback.

Test locally:

```bash
cd mobile/packages/sound_service
flutter test
```
