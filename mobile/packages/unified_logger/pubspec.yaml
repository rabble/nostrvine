name: unified_logger
description: Structured logging with category/level filtering and in-memory capture.
version: 0.1.0+1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  models:
    path: ../models

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^10.2.0
