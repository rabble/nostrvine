name: video_event_cache
description: >-
  Abstract interface for video event caching.
  Decouples consumers from the concrete VideoEventService implementation.
version: 0.1.0+1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  models:
    path: ../models

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
  very_good_analysis: ^10.2.0
