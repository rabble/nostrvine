# nostr_key_manager

Status: Current
Validated against: `pubspec.yaml` on 2026-03-19.

Purpose: secure Nostr key management with device-backed storage and bunker support.

Used by: authentication, key backup, and signing flows.

Test locally:

```bash
cd mobile/packages/nostr_key_manager
flutter test
```
