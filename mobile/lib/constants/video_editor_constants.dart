class VideoEditorConstants {
  static String autoSaveId = 'draft_autosave';
  static int tagLimit = -1;
  static bool enableTagLimit = false;
  static const maxDuration = Duration(seconds: 6, milliseconds: 300);
}
